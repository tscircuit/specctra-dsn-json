import type { ExecutionContext } from "ava"
import { readFileSync } from "node:fs"

/**
 * The standard `getTestFixture` function here doesn't have a purpose yet...
 */
export const getTestFixture = (t: ExecutionContext) => {}
